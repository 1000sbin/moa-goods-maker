# 모아 굿즈메이커 — 1.7 ~ 2.0 로드맵

> 아직 확정 아님. 모디가 적어둔 것 + 지금 코드 기준으로 "이미 있는 것 / 없는 것"만 정리한 메모.
> 착수할 때 이 문서부터 읽고, 순서·범위는 그때 다시 정한다.

---

## 1. 여러 이미지를 한 번에 불러와서 **편집**

**이미 있는 것**: `runBatch()` — 여러 파일을 받아 각각 같은 설정으로 계산해서 ZIP으로 떨어뜨린다.
**없는 것**: 편집. 지금은 미리보기도 없고 이미지별로 다르게 만질 수 없다 (`S.img` 하나만 잡고 순차 처리 후 원상복구).

필요한 구조 변경:
- `S.img` / `S.W` / `S.H` 단일 슬롯 → **이미지 목록 + 활성 인덱스**
- 이미지별 설정 분리. 어디까지 개별로 둘지가 설계 포인트:
  - 개별이어야 하는 것: 위치, 촉/타공 오프셋, 실물 크기
  - 공용이어야 하는 것: DPI, 여백, 둥글리기, 칼선 색 (인쇄소 제출 규격이라 섞이면 안 됨)
- 썸네일 스트립 UI + 이미지 추가/삭제/순서
- 내보내기: 이미지별 폴더(현재 방식) / 한 판에 합쳐서(조판) 중 무엇이 기본인지

> ⚠️ `computeCore`가 전역 `S`를 직접 읽고 쓰는 구조라 여기가 제일 큰 리팩터. 1.7에서 이것부터
> 하지 않으면 아래 항목(디오라마·양면·다중 슬롯)이 전부 이 위에 얹히므로 순서상 **선행 작업**.

---

## 2. 인물별 개별 이동

**이미 있는 것**: 인물 인식은 됨 (`personComps()` — 작은 파편은 큰 덩어리에 흡수). 촉·타공·고리는
이미 **인물별 개별 오프셋**을 갖는다 (`tabOffs` / `holeOffs` / `earOffs`, Shift+클릭 다중 선택도 됨).

**없는 것**: **인물 자체(그림 픽셀)를 따로 옮기는 것.** 지금 이미지 이동은 `imgOffX/imgOffY` 하나뿐이라
그림 전체가 같이 움직인다.

필요한 것:
- 인물별 마스크를 잘라 독립 레이어로 승격 → 인물마다 위치 오프셋
- 옮긴 뒤 **겹침·간격 검사** (칼선끼리 붙으면 재단 불가)
- 원본 이미지의 어느 픽셀이 어느 인물인지 유지 (`labelComponents` 결과를 계산마다 버리지 않고 캐시)

---

## 3. 스탠드 — 옆 간격(간섭) 검사

**이미 있는 것**: 받침이 겹치거나 1mm 이내로 붙으면 하나로 병합하는 그룹핑 로직.

**없는 것**: "안 붙는지"의 **판정과 경고**. 병합은 받침 얘기고, 세웠을 때 인물끼리 부딪히는지는 안 본다.

필요한 것:
- 인물 실루엣 간 최소 수평 간격 측정 → 기준 미달이면 경고 (타공 근접 경고와 같은 방식)
- 슬롯 간 최소 거리 (촉 두께 + 아크릴 두께 + 여유)
- 아마 "권장 간격 자동 배치" 버튼

---

## 4. 스탠드 다중 슬롯 — **앞뒤 간격** 모드

모디 원문: "여러 장을 한 번에 넣는 거라면 촉을 따라서 구멍을 움직이면 안 되고 앞뒤 간격만 재야 하는
경우도 있을 텐데"

지금 슬롯은 각 인물의 촉 x좌표를 그대로 따라간다 (`c.tabCx` 기준). 여러 장을 한 받침에 세우는
구성에서는 그게 아니라 **깊이(앞뒤) 방향 배치**가 필요하다.

→ 받침 슬롯 배치 모드 두 가지:
- **촉 따라가기** (현재) — 한 판 안의 여러 인물
- **앞뒤 간격 지정** (신규) — 여러 장을 층으로. 슬롯이 받침 안에서 앞/뒤로 나열되고,
  간격은 `아크릴 두께 + 여유`로 계산. 3번(간섭 검사)과 같은 계산을 공유.

이게 사실상 디오라마의 절반이므로 6번과 함께 설계하는 게 맞다.

---

## 5. 아크릴 쉐이커

파트 3장 구성 (앞판 / 스페이서 / 뒷판) + 내용물 주입구.

필요한 것:
- **챔버 라인**: 외곽에서 안쪽으로 오프셋한 폐곡선 = 글리터가 도는 공간.
  기존 거리변환(등고선) 파이프라인을 안쪽으로 쓰면 그대로 나온다 — 마이너스 여백이 이미 됨.
- **스페이서(테두리) 파트**: 외곽 − 챔버 = 링. 별도 조각으로 내보내기.
  링 최소 폭(접착면) 검사 필요 — 좁으면 새거나 안 붙음.
- 챔버가 여러 조각으로 갈라지는지 검사 (실루엣이 얇은 부분에서 링이 끊김)
- 파트별 레이어/파일 분리 (앞판·스페이서·뒷판)

열린 질문: 인쇄소마다 요구 규격(스페이서 두께, 접착 방식, 주입구 유무)이 다를 것 → 프리셋 필요?

---

## 6. 디오라마

여러 장을 깊이 방향으로 세우는 구성. 4번(앞뒤 간격 슬롯) + 1번(다중 이미지) 위에 얹힌다.

필요한 것:
- 레이어 순서 (앞 → 뒤) 지정 UI
- 층마다 촉 위치·크기 개별 지정
- 받침 하나에 슬롯 N개, 앞뒤 간격 계산
- 뒤 레이어가 앞 레이어에 가려지는 부분 미리보기 (겹침 시각화)
- 배경판(가장 뒤 판)을 실루엣이 아닌 사각/도형으로 뽑는 옵션 — 도형 기능 재활용 가능

---

## 7. 양면 아크릴

앞뒤 다른 그림. 지금 화이트 레이어 로직을 확장하는 형태가 될 것.

필요한 것:
- 뒷면 이미지 입력 + **좌우 반전** (뒷면은 뒤집어 인쇄)
- 앞/뒤 정합: 실루엣이 다르면 칼선을 어느 쪽 기준으로 딸지 (합집합? 앞면 기준?)
- **차단 화이트**: 앞뒤가 서로 비쳐 보이지 않게 가운데 화이트를 꽉 채워 깔아야 한다.
  현재 화이트는 "앞면 실루엣 기반"이므로 양면일 때는 **앞뒤 합집합**이 되어야 함.
- 레이어 출력 순서: 디자인(앞) / 화이트 / 디자인(뒤, 반전) — PSD·PDF 레이어명도 그에 맞게
- 미리보기에서 앞/뒤 토글

열린 질문: 정합 오차(앞뒤 인쇄가 살짝 어긋남)를 위해 화이트를 몇 mm 물려야 하는지 — 인쇄소 기준 확인 필요.

---

## 8. 그림 회전 — ✅ v1.6.2에서 완료

임의 각도로 구현됨. 회전을 원본에 "구워서" `S.img`로 넘기는 방식이라 이후 파이프라인은 회전을 모른다.
아래는 착수 전 메모 (구현 결과와 대조용).

넣을 곳: `prepareSource()`에서 작업 캔버스에 그릴 때 회전. 마스크·칼선·화이트가 전부 그 캔버스에서
파생되므로 자동으로 따라온다. 디자인 레이어 내보내기도 같은 변환을 써야 함.

주의할 것:
- **90/180/270°는 무손실 변환으로 따로 처리.** 임의 각도는 알파를 리샘플링해서 외곽이 물러지고,
  그만큼 감도(threshold) 결과가 달라진다. 직각 회전이 실사용 대부분일 테니 이쪽을 우선.
- `S.pad` 를 회전된 bbox 기준으로 다시 잡아야 한다 (회전하면 외접 사각형이 커짐).
- **실물 크기(mm) 입력과 대지 크기가 어느 기준인지** 정해야 함 — 회전 전 원본이냐 회전 후 bbox냐.
  회전 후 기준이 직관적이지만, 회전할 때마다 숫자가 바뀌는 게 혼란스러울 수도 있다.
- 오뚝이와 궁합이 좋다: 회전하면 무게중심 위치가 바뀌고 반지름·접지점이 자동으로 다시 계산된다.
  자세가 기울어진 그림을 **똑바로 세우는 용도**로 쓸 수 있음.
- 2번(인물별 이동)이 들어가면 회전도 인물별로 갖고 싶어질 것 → 그때 오프셋과 같은 자리에 각도를 둔다.

## 순서 제안 (초안)

1. **1번 다중 이미지 리팩터** — 나머지가 다 이 위에 올라간다
2. **2번 인물별 이동** + **3번 간섭 검사** — 같은 계산(인물 마스크·간격)을 공유
3. **4번 앞뒤 간격 슬롯** → **6번 디오라마**
4. **7번 양면** (화이트 로직 확장, 독립적이라 언제든 끼울 수 있음)
5. **5번 쉐이커** (파트 분리 개념이 새로 들어와서 제일 독립적 = 제일 나중이어도 됨)

**8번 회전은 v1.6.2에서 완료.** 예상대로 의존성이 없어 단독으로 들어갔다.

## 배포할 때 잊지 말 것 (기존 함정)

- 버전 3곳 동기화: `package.json` / html 푸터 배지 / `const APP_VER`
- 릴리스 파일명은 전부 ASCII (`MoaGoodsMaker-*`) — 한글 파일명은 깃허브가 지워서 자동 업데이트 404
- 새 UI 문자열은 `TR_EN` / `TR_JA` / `TR_ZH` 세 곳에 다 넣어야 언어 전환에 잡힌다
  (`captureKo`가 `TR_EN`에 키가 있는 텍스트만 감싼다)
